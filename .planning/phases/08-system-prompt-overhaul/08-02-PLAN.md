---
phase: 08-system-prompt-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: []
autonomous: false
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
  - PROMPT-05
  - PROMPT-06
  - PROMPT-07
  - PROMPT-08
  - PROMPT-09
  - BTN-01
  - BTN-02
  - BTN-03
  - BTN-04
  - CONV-01
  - CONV-02
  - CONV-03
  - CONV-04

must_haves:
  truths:
    - "User can ask about working hours and receive a correct answer with a booking nudge"
    - "User can ask about prices and receive a formatted answer from get_services with a booking nudge"
    - "User can ask about location and receive the actual address with a booking nudge"
    - "User can ask about payment and receive 'naqd pul yoki karta' answer with a booking nudge"
    - "User greeting (salom) is handled naturally without booking flow or nudge"
    - "Unknown question redirects to business phone number without guessing"
    - "Before create_booking is called, user sees a summary with confirmation buttons"
    - "Buttons appear on at most 5 of 15 representative test messages"
    - "Each AI response contains at most one question"
  artifacts: []
  key_links: []
---

<objective>
Verify the system prompt overhaul works correctly in a live end-to-end booking flow. This is a human verification checkpoint — the user runs the chat on a real tenant page and confirms all behavioral requirements are met.

Purpose: Phase 8 modifies the live booking flow. Unit tests verify prompt structure, but only a real conversation with the AI can confirm the prompt produces correct behavior. STATE.md flags this phase as risk-bearing.

Output: Verified confirmation that all 17 requirements work in practice.
</objective>

<execution_context>
@/Users/shahzod/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shahzod/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-system-prompt-overhaul/08-CONTEXT.md
@.planning/phases/08-system-prompt-overhaul/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start blyss-gcloud-api dev server and measure prompt token count</name>
  <files></files>
  <action>
1. Start the API dev server:
   ```
   cd /Users/shahzod/Projects/BLYSS/blyss-gcloud-api && npm run dev
   ```
   (Run in background — the human will use the chat on the tenant page.)

2. Measure the system prompt token count. Read the chatAi.js file, extract the template literal from `buildChatSystemPrompt()`, and estimate tokens:
   - Count the characters in the template literal (excluding interpolated variable values)
   - Divide by 4 for rough token estimate
   - If over 800 tokens (~3200 chars of static text), report which sections to trim

3. Report the token count estimate to the user before the verification checkpoint.
  </action>
  <verify>
    <automated>cd /Users/shahzod/Projects/BLYSS/blyss-gcloud-api && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Dev server is running. Prompt token count is measured and reported. If over budget, specific trimming recommendations are provided.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of all 17 requirements in live chat</name>
  <action>Human verifies the system prompt overhaul by running 15 representative test messages through the live chat widget and checking all behavioral requirements.</action>
  <what-built>Complete system prompt overhaul — the AI now answers customer questions naturally, enforces a confirmation step before booking, and limits buttons to 3 specific scenarios</what-built>
  <how-to-verify>
Open the chat on a tenant page (e.g., http://localhost:3000 or a tunnel URL). Run these 15 representative test messages one at a time and verify each response:

**Q&A Tests (expect natural answer + booking nudge, NO buttons):**
1. "Qachon ishlaysizlar?" — expect actual working hours from data, nudge appended
2. "Narxlar qancha?" — expect get_services called, prices listed naturally, nudge
3. "Qayerdasizlar?" — expect actual address from business data, nudge
4. "Qanday to'lov qabul qilasiz?" — expect "naqd pul yoki karta", nudge
5. "Borsam bo'ladimi?" — expect walk-in policy answer, nudge
6. "Bekor qilsam bo'ladimi?" — expect phone redirect, nudge

**Greeting/Small Talk Tests (expect natural reply, NO nudge, NO booking flow):**
7. "Salom" — expect a short greeting reply, no nudge
8. "Rahmat" — expect polite acknowledgment, no nudge
9. "Ko'rishguncha" — expect farewell, no nudge

**Unknown/Off-topic Tests (expect phone redirect, NO guessing):**
10. "WiFi bormi?" — expect redirect to phone number
11. "Futbol haqida nima deysan?" — expect redirect to business services

**Booking Flow Test (expect confirmation step with action-labeled buttons):**
12. "Soch oldirmoqchiman" — expect "qaysi kunga?" (no buttons unless multiple services)
13. Select a day — expect time slots with buttons
14. Select a time — expect CONFIRMATION SUMMARY with 2 action-labeled buttons ("Ha, yozib qo'ying" / "Vaqtni o'zgartiraman"), NOT "Ha/Yo'q"
15. Tap confirm button — expect create_booking called and success message

**Verification Checklist:**
- [ ] Working hours answer matches actual business data (PROMPT-01)
- [ ] Prices come from get_services, not hallucinated (PROMPT-02)
- [ ] Address matches business location data (PROMPT-03)
- [ ] Payment answer is "naqd pul yoki karta" (PROMPT-04)
- [ ] Walk-in answer recommends booking (PROMPT-05)
- [ ] Cancellation redirects to phone (PROMPT-06)
- [ ] Greetings handled naturally, no booking flow triggered (PROMPT-07)
- [ ] Unknown questions redirect to phone, no guessing (PROMPT-08)
- [ ] Nudge appears after informational answers but NOT after greetings (PROMPT-09)
- [ ] Confirmation summary + action-labeled buttons shown before booking (BTN-01, BTN-02)
- [ ] Buttons appear on at most 5 of the 15 responses (BTN-03)
- [ ] No buttons on phone/OTP/name input requests (BTN-04)
- [ ] Each response asks at most one question (CONV-01)
- [ ] AI responds in the language the user writes in (CONV-02)
- [ ] Tone is natural, not robotic (CONV-03)
- [ ] Business context (address, hours, payment) present in prompt (CONV-04)

**Uzbek Cyrillic phrasing note:** If any Uzbek text looks unnatural, note specific phrases for correction.
  </how-to-verify>
  <verify>Human confirms all 17 requirements pass in live conversation</verify>
  <done>All 17 requirements verified in live E2E test. Human approved the system prompt overhaul.</done>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found (e.g., "PROMPT-01 failed: hours showed wrong format", "BTN-03 failed: buttons appeared on 8 of 15 responses")</resume-signal>
</task>

</tasks>

<verification>
1. All unit tests still pass: `cd /Users/shahzod/Projects/BLYSS/blyss-gcloud-api && npm test`
2. Human verified 15-message E2E test confirms all 17 requirements
3. Prompt token count is within 800-token budget
4. No booking was created without the confirmation step
</verification>

<success_criteria>
- All 17 requirements (PROMPT-01 through CONV-04) verified in a live conversation
- Confirmation gate prevents booking without explicit user confirmation
- Buttons appear on at most 5 of 15 representative responses
- Nudge appears only after informational answers, not after greetings/farewells
- Prompt stays within token budget
- Human approves the E2E verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-system-prompt-overhaul/08-02-SUMMARY.md`
</output>
