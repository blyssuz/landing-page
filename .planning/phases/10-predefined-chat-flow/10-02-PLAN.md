---
phase: 10-predefined-chat-flow
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/api/chat/route.ts
autonomous: false
requirements: [CLN-01]

must_haves:
  truths:
    - "The /api/chat proxy route file is deleted from the codebase"
    - "The entire predefined chat flow works end-to-end: language select -> menu -> responses -> back to menu"
    - "No dead imports or references to deleted files exist"
  artifacts:
    - path: "app/api/chat/route.ts"
      provides: "DELETED — must not exist"
  key_links:
    - from: "app/[locale]/[tenant]/_components/ChatWidget.tsx"
      to: "(none)"
      via: "No external API calls — fully self-contained"
      pattern: "No fetch calls in file"
---

<objective>
Delete the now-unused /api/chat proxy route and verify the full predefined chat flow works end-to-end.

Purpose: Complete the cleanup by removing dead API code that is no longer called by anything, and visually confirm the entire flow works as expected.

Output: Deleted route file, verified working chat flow.
</objective>

<execution_context>
@/Users/shahzod/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shahzod/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-predefined-chat-flow/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete /api/chat route and verify no dangling references</name>
  <files>app/api/chat/route.ts</files>
  <action>
Delete the file `app/api/chat/route.ts`. This was the Next.js API proxy route that forwarded chat messages to the Express backend for OpenAI processing. Since ChatWidget no longer makes any fetch calls to `/api/chat`, this file is dead code.

After deletion, verify no other files reference `/api/chat`:
- `grep -r "api/chat" app/ --include="*.ts" --include="*.tsx"` should return zero results
- `grep -r "api/chat" lib/ --include="*.ts"` should return zero results (if lib/ exists)

Also verify the `chatAutoLogin` import in ChatWidget.tsx was removed in Plan 01. If somehow still present, remove it from ChatWidget.tsx (the import `import { chatAutoLogin } from '../actions'`). The `chatAutoLogin` function in `actions.ts` itself should NOT be deleted — it may be used by other code paths. Only remove the import from ChatWidget if it still exists.

Run `npx tsc --noEmit` to confirm no compilation errors after deletion.
Run `npm run build` to confirm the production build succeeds.
  </action>
  <verify>
    <automated>cd /Users/shahzod/Projects/BLYSS/landing-page && test ! -f app/api/chat/route.ts && npx tsc --noEmit --pretty 2>&1 | head -20 && echo "PASS: route deleted and types clean"</automated>
  </verify>
  <done>
- `app/api/chat/route.ts` does not exist
- No file in `app/` references `/api/chat`
- TypeScript compiles without errors
- Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete predefined chat flow end-to-end</name>
  <action>
Human verifies the full predefined chat flow works correctly in both languages with real business data.

What was built:
1. ChatWidget opens with UZ/RU language selection buttons
2. After language selection, greeting + main menu buttons appear
3. Each menu button (Prices, Services, Location, Working Hours, Contact) shows real business data
4. "Back to menu" button returns to main menu from any response
5. Typing indicator animation shows between button tap and response
6. No text input area — all interaction is via buttons
7. /api/chat route has been deleted
  </action>
  <verify>
How to verify:
1. Start dev server: `npm run dev`
2. Open a tenant page in browser (e.g., http://localhost:3000/uz/barber-house or whichever tenant exists)
3. Tap the chat bubble (bottom-right floating button)
4. Verify: Two buttons appear — "O'zbekcha" and "Русский" — no greeting text yet
5. Tap "O'zbekcha"
6. Verify: Greeting message appears ("Assalomu alaykum!...") followed by 5 menu buttons
7. Tap "Narxlar"
8. Verify: "Narxlar" appears as your message, typing dots show briefly, then actual service prices appear
9. Tap "Bosh menyu"
10. Verify: Returns to main menu with all 5 buttons
11. Tap "Ish vaqti" — verify real working hours appear
12. Tap "Manzil" — verify real address appears
13. Tap "Bog'lanish" — verify phone number appears
14. Close chat, reopen — verify language selection appears again (session reset)
15. Tap "Русский" this time — verify all text is in Russian
16. Try visiting http://localhost:3000/api/chat — should 404 (route deleted)
  </verify>
  <done>Human has confirmed the complete predefined chat flow works end-to-end with real business data in both Uzbek and Russian languages. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
1. `app/api/chat/route.ts` does not exist on disk
2. `grep -r "api/chat" app/ --include="*.ts" --include="*.tsx"` returns nothing
3. `npm run build` succeeds
4. Human verified the complete flow works end-to-end
</verification>

<success_criteria>
- Dead API route is deleted
- No dangling references to /api/chat
- Build passes
- Human has verified the complete predefined chat flow works with real business data in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/10-predefined-chat-flow/10-02-SUMMARY.md`
</output>
